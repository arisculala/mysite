version: "3.9"

services:
  app:
    image: arisculala/mysite:latest
    container_name: mysite-app
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3000
    ports:
      - "127.0.0.1:3000:3000"
